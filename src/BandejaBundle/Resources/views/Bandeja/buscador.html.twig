{{ form_start(searchForm, {'attr': {'class': 'col', 'novalidate': 'novalidate'}}) }}
{{ form_widget(searchForm.searchText, {'attr': {'class': 'form-control', 'placeholder': 'Buscar...'}}) }}
{{ form_end(searchForm) }}
